export default function SectionTitle({ title }) {
    return (
        <div className='shadow-cta p-3 flex flex-col items-center w-fit'>
            <h2 className="text-3xl">{title}</h2>
        </div>
    )
}